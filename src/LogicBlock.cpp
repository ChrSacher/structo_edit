#include "LogicBlock.h"


std::shared_ptr<Element> LogicBlock::execute(VariableMap& variables)
{
	// could evaluate logical expression - not now, since we dont focus on execution

	return std::shared_ptr<Element> (nullptr);
}
